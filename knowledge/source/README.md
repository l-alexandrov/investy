# Source material

Drop your raw course/book material here — slides, exported Excel/CSV tables, lecture transcripts. This folder is only ever read during the one-time (or occasionally re-run) guide-compilation step described in the root `CLAUDE.md` — no agent reads it automatically.
